/**
 * Provides setTimeout hook.
 *
 * @param cb callback to run after delay
 * @param timeout the timeout delay (in ms)
 */
export declare function useTimeout(cb: Function, timeout: number | null): void;
